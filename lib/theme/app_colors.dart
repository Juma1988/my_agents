import 'package:flutter/material.dart';

/// Centralized color constants for the app.
class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF1A1A2E);
  static const Color surface = Color(0xFF1E1E2E);
  static const Color surfaceAlt = Color(0xFF16162A);

  // Accent
  static const Color accent = Color(0xFF4ECDC4);
  static const Color accentDark = Color(0xFF2AB7AD);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Colors.white54;
  static const Color textMuted = Colors.white38;

  // Timer
  static const Color timerOrange = Color(0xFFFF8C42);

  // Timer warning
  static const Color timerWarning = Color(0xFFFF4444);
}
